"""brain_ingest — ingestion pipeline for a personal second brain.

Pipeline: scan -> extract -> classify (manifest filled by Claude Code) ->
chunk -> ingest-graph (Graphiti + FalkorDB).
"""

__version__ = "0.1.0"
